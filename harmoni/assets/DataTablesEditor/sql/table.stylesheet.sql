-- 
-- Editor SQL for DB table stylesheet
-- Created by http://editor.datatables.net/generator
-- 

CREATE TABLE IF NOT EXISTS `stylesheet` (
	`forfapeda` int(10) NOT NULL auto_increment,
	PRIMARY KEY( `forfapeda` )
);